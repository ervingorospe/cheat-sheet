import { styled, YStack } from "tamagui";

export const AppTextStack = styled(YStack, {
  name: "AppTextStack",

  gap: "$sm",
});

export const AppList = styled(YStack, {
  name: "AppList",

  gap: "$xl",
});

export const AppSection = styled(YStack, {
  name: "AppSection",

  gap: "45",
});

export const AppButtonGroup = styled(YStack, {
  name: "AppButtonGroup",

  gap: "$md",
});

export const AppFormInputs = styled(YStack, {
  name: "AppButtonGroup",

  gap: "$4",
});

export const FormSection = styled(YStack, {
  name: "AppButtonGroup",

  gap: "$lg",
});
